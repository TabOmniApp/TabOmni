// Scratch file used for a one-off visual check outside Electron; unused by
// the app itself. Left inert (rather than deleted — `rm` isn't permitted in
// this sandbox) so it doesn't break `tsc` for the rest of the project.
export {}
